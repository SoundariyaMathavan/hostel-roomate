#!/bin/bash

# Navigate to the project directory
cd "$(dirname "$0")"

# Apply migrations using Django's manage.py
echo "Applying migrations..."
python manage.py migrate

# Check if the migration was successful
if [ $? -eq 0 ]; then
    echo "Migration applied successfully!"
else
    echo "Migration failed. Please check the error message above."
    exit 1
fi

echo "You can now restart your Django server."#!/bin/bash

# Navigate to the project directory
cd "$(dirname "$0")"

# Apply migrations using Django's manage.py
echo "Applying migrations..."
python manage.py migrate

# Check if the migration was successful
if [ $? -eq 0 ]; then
    echo "Migration applied successfully!"
else
    echo "Migration failed. Please check the error message above."
    exit 1
fi

echo "You can now restart your Django server."#!/bin/bash

# Navigate to the project directory
cd "$(dirname "$0")"

# Apply migrations using Django's manage.py
echo "Applying migrations..."
python manage.py migrate

# Check if the migration was successful
if [ $? -eq 0 ]; then
    echo "Migration applied successfully!"
else
    echo "Migration failed. Please check the error message above."
    exit 1
fi

echo "You can now restart your Django server."#!/bin/bash

# Navigate to the project directory
cd "$(dirname "$0")"

# Apply migrations using Django's manage.py
echo "Applying migrations..."
python manage.py migrate

# Check if the migration was successful
if [ $? -eq 0 ]; then
    echo "Migration applied successfully!"
else
    echo "Migration failed. Please check the error message above."
    exit 1
fi

echo "You can now restart your Django server."#!/bin/bash

# Navigate to the project directory
cd "$(dirname "$0")"

# Apply migrations using Django's manage.py
echo "Applying migrations..."
python manage.py migrate

# Check if the migration was successful
if [ $? -eq 0 ]; then
    echo "Migration applied successfully!"
else
    echo "Migration failed. Please check the error message above."
    exit 1
fi

echo "You can now restart your Django server."#!/bin/bash

# Navigate to the project directory
cd "$(dirname "$0")"

# Apply migrations using Django's manage.py
echo "Applying migrations..."
python manage.py migrate

# Check if the migration was successful
if [ $? -eq 0 ]; then
    echo "Migration applied successfully!"
else
    echo "Migration failed. Please check the error message above."
    exit 1
fi

echo "You can now restart your Django server."