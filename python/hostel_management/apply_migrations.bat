@echo off
echo Applying migrations...
python manage.py migrate

if %ERRORLEVEL% EQU 0 (
    echo Migration applied successfully!
) else (
    echo Migration failed. Please check the error message above.
    exit /b 1
)

echo You can now restart your Django server.@echo off
echo Applying migrations...
python manage.py migrate

if %ERRORLEVEL% EQU 0 (
    echo Migration applied successfully!
) else (
    echo Migration failed. Please check the error message above.
    exit /b 1
)

echo You can now restart your Django server.@echo off
echo Applying migrations...
python manage.py migrate

if %ERRORLEVEL% EQU 0 (
    echo Migration applied successfully!
) else (
    echo Migration failed. Please check the error message above.
    exit /b 1
)

echo You can now restart your Django server.@echo off
echo Applying migrations...
python manage.py migrate

if %ERRORLEVEL% EQU 0 (
    echo Migration applied successfully!
) else (
    echo Migration failed. Please check the error message above.
    exit /b 1
)

echo You can now restart your Django server.python manage.py migrate

if %ERRORLEVEL% EQU 0 (
    echo Migration applied successfully!
) else (
    echo Migration failed. Please check the error message above.
    exit /b 1
)

echo You can now restart your Django server.echo Applying migrations...
python manage.py migrate

if %ERRORLEVEL% EQU 0 (
    echo Migration applied successfully!
) else (
    echo Migration failed. Please check the error message above.
    exit /b 1
)

echo You can now restart your Django server.echo You can now restart your Django server.echo Applying migrations...
python manage.py migrate

if %ERRORLEVEL% EQU 0 (
    echo Migration applied successfully!
) else (
    echo Migration failed. Please check the error message above.
    exit /b 1
)

echo You can now restart your Django server.